parameters: {}
environment: {}
packages:
    - name: geih
      shared: false
      environment:
        PROTOCOL: "${PROTOCOL}"
        DRIVER: "${DRIVER}"
        USER: "${USER}"
        PASSWORD: "${PASSWORD}"
        HOST: "${HOST}"
        PORT: "${PORT}"
        DB: "${DB}"
        SSLMODE: "${SSLMODE}"
      parameters: {}
      annotations: {}
      functions:
        - name: income
          binary: false
          main: ""
          runtime: python:3.11
          web: true
          webSecure: false
          parameters: {}
          environment: {}
          annotations: {}
          limits: {}
